import 'package:flutter/material.dart';

class ProfileFrontPage extends StatelessWidget {
  const ProfileFrontPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Profile front Page.')),
    );
  }
}

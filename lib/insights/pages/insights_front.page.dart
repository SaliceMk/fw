import 'package:flutter/material.dart';

class InsightsFrontPage extends StatelessWidget {
  const InsightsFrontPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Insights front page.')),
    );
  }
}
